export interface Currency {
      align?: string,
      allowNegative?: boolean,
      decimal?: string,
      precision?: number,
      prefix?: string,
      suffix?: string,
      thousands?: string
}